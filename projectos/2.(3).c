#include <stdio.h>
/* promedio curso.
el programa, al recobor como dato el promedio de un alumno en un curso universitario, escribe aprobado si su promedio es mayo o igual 6, 0 reprobado en caso contrario.
PRO: variable de tipo real. */
void main (void)
{
    float PRO;
    printf("Ingrese el promedio del alumno: ");
    scanf("f", &pro);
    if (PRO >= 6.0)
        printf("\nAprobado");
        else
            printf("\nReprobado");
}
