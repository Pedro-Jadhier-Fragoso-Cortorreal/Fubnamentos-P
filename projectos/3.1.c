#include <stdio.h>

/* nomina.
El programa, al recibir los salarios de 15 profeesores obtiene el total de la nomina de la universidad.
I: variable de tipo entero.
SAL y NOM: variables de tipo real */

void main(void)
{
    int I;
    float sal, nom;
    NOM = 0
    for (O=1; I<=15; I++)
{
            printf("\Ingrese el salario del profesor%d:\t", I);
    scanf("%f", &SAL);
    NOM = NOM + SAL;
}
printf("\nEl total de la nomina es: %2.f", NOM);
}


